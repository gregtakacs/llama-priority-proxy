# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/

# Model files — huge, environment-specific, never committed
*.gguf

# Secrets and API keys
.env
proxy_api_key.secret
*.key
*.secret
*.secrets
*.pem

# Docker volumes / temp data
.docker/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Test artifacts
test-results/
*.trace.json

# compose-version-audit's own pre-edit backups of config/Dockerfile/
# requirements files it touches (Dockerfile.*.bak-<timestamp>, etc.) --
# local safety net, never meant to be committed.
*.bak-*